# Backend configuration file
# Use this if you prefer to keep backend config separate from main.tf
# Initialize with: terraform init -backend-config=backend.hcl

bucket         = "aws-glue-assets-651914028873-us-east-1"
key            = "terraform/glue-schema-registry/terraform.tfstate"
region         = "us-east-1"
encrypt        = true
dynamodb_table = ""  # Optional: Add DynamoDB table name for state locking

# To use with a different bucket/region:
# bucket = "your-terraform-state-bucket"
# key    = "terraform/glue-schema-registry/terraform.tfstate"
# region = "us-east-1"
